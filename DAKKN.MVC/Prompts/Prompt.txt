I'm working on the DAKKN repo (ASP.NET Core MVC, Clean Architecture). Localization is handled via `IStringLocalizer<Messages>` injected in views (see Views/_ViewImports.cshtml), backed by a custom JsonStringLocalizerFactory (DAKKN.Application/Localization/JsonStringLocalizerFactory.cs) that reads from DAKKN.Application/Localization/Resources/messages.en.json and messages.ar.json.

Fix all localization issues in the landing page views and its layout, specifically:
- Views/Shared/_LayoutLanding.cshtml
- Views/Home/Index.cshtml, About.cshtml, Privacy.cshtml, Terms.cshtml
- Views/Home/_HeroSection.cshtml, _Navbar.cshtml, _CategoriesSection.cshtml, _FeaturedProductsSection.cshtml, _AllProductsSection.cshtml, _StatsSection.cshtml, _HowItWorks.cshtml, _TestimonialsSection.cshtml, _FaqSection.cshtml, _ContactSection.cshtml, _FooterSection.cshtml, _SignInModal.cshtml
- Views/Shop/Products.cshtml, ProductDetails.cshtml, Cart.cshtml, Categories.cshtml

Tasks:
1. Find every hardcoded English (or Arabic) string in markup, attributes (placeholder, title, alt, aria-label), and any inline <script> blocks in these files, and replace it with @Localizer["key"].
2. For every new key you introduce, add it to BOTH Resources/messages.en.json and messages.ar.json with accurate, natural translations (don't leave placeholder text). Keep key naming consistent with existing conventions (e.g. hero_*, nav_*, footer_*).
3. Reuse existing keys instead of duplicating near-identical strings — search messages.en.json first.
4. Check that _LayoutLanding.cshtml correctly sets `dir="rtl"`/`dir="ltr"` and `lang` on <html> based on CurrentUICulture, and that any layout-level text (meta tags, structured data, JS that injects text) is also localized.
5. Verify Views/Shared/Partials/_LanguageToggle.cshtml, _ThemeToggle.cshtml, _Preloader.cshtml used in this layout have no leftover hardcoded text.
6. Double check JS/CSS in these views isn't injecting English-only strings (e.g. toast/alert messages, validation messages, "Add to cart" type buttons rendered via JS).
7. After your changes, scan the same files again for any remaining literal text inside HTML tags that should be localized but wasn't caught.

Don't touch Customer or Admin views/layouts in this pass. Show me a summary of every key added/reused at the end.