Work in the DAKKN ASP.NET Core MVC project. Focus only on the admin area: 
Views/Shared/_LayoutAdmin.cshtml and every view under Views/Admin/ 
(Dashboard, AddCategory, AddProduct, AddShippingGovernorate, BrandReviews, 
Categories, CategoryProducts, Content, Inventory, OrderDetails, Orders, 
Placeholder, ProductDetails, Settings, Shipping, Support, Users, and partials 
_UserListPartial, _UserStatsPartial, _UserTablePartial), plus 
wwwroot/css/admin.css and wwwroot/css/common.css.

1. Refine the color palette for the admin dashboard: it should read as a serious, 
   data-dense back-office tool — calmer and more neutral than the customer/landing 
   experience, while still anchored to the brand teal (--primary-color: #0E908C, 
   --primary-rgb) for active states, key actions, and highlights. Establish clear, 
   consistent colors for status badges (e.g. status-shipped and similar classes in 
   admin.css), charts/stats cards, alerts, and table row states (hover/selected). 
   Fix any low-contrast or inconsistent color usage currently in admin.css.
2. Enhance the design: improve the Dashboard's stat cards and charts layout, 
   standardize the sidebar/nav styling, table density and readability in 
   Orders/Inventory/Users/Categories, modal and form styling in Settings/Content, 
   and make sure scrollbar/focus styles (already partially defined in admin.css) 
   are consistent across all admin pages.
3. Make all admin views responsive — admin.css currently has almost no media 
   queries. Add proper breakpoints so the sidebar collapses to a drawer/hamburger 
   on smaller screens, tables become scrollable or stack on mobile, and 
   dashboard stat cards/charts reflow into a single column on narrow viewports.
4. Do not change controller logic, authorization filters, ViewModels, or form 
   submission targets — only CSS/Tailwind classes and markup needed for visual 
   consistency and responsiveness.

Show me a summary of every file changed and why before finalizing.